query State {
  getBuddyProjectStatus {
    buddy @export(exportName: "Buddy") {
      userId
      username
    }
    status
  }
}

mutation SignUp {
  buddyProjectSignUp {
    result

    status {
      status
    }
  }
}
