kind: Service
apiVersion: v1
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ .Values.cms.service.name }}
  labels: {{ .Values.cms.service.labels | toYaml | nindent 4 }}
spec:
  ports:
    - port: 80
      targetPort: {{ .Values.cms.deployment.port.name }}
  selector: {{ .Values.cms.service.labels | toYaml | nindent 4 }}
